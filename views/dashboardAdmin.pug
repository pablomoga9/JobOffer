doctype html
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(http-equiv="X-UA-Compatible", content="IE=edge")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title Document
    body 

         form#adForm(action="/api/ads" method="POST") 
            input#job(type="text" name="job")
            input#title(type="text" name="title")
            input#city(type="text" name="city")
            input#company(type="text" name="company")
            input#description(type="text" name="description")
            button#submitBtn(type="submit" value="Search") Enviar 

    if created
        h2#successMessage Anuncio publicado con éxito 
        a#backToDashboard(href="/dashboard") Volver